<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tobiapplications.com.moviebase">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".utils.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.CoverPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.overview.OverviewActivity">
            <meta-data
                android:name="android.app.default_searchable"
                android:value=".ui.search.SearchActivity" />
        </activity>
        <activity
            android:name=".ui.detail.DetailActivity"
            android:configChanges="orientation|screenSize"
            android:parentActivityName=".ui.overview.OverviewActivity"
            android:theme="@style/CustomToolbarTitle" >

        </activity>
        <activity android:name=".ui.search.SearchActivity"
            android:parentActivityName=".ui.overview.OverviewActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:parentActivityName=".ui.overview.OverviewActivity"></activity>


        <!-- Provider -->
        <provider
            android:name=".database.MoviesProvider"
            android:authorities="tobiapplications.com.moviebase"
            android:exported="false" />

    </application>

</manifest>